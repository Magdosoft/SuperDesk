###open files with root password
gksu nautilus

###user mangment
sudo apt-get install gnome-system-tools

sudo usermod -a -G groupName userName

###stop /restart superdesk 
sudo systemctl stop superdesk
sudo systemctl start superdesk
sudo systemctl restart superdesk
sudo systemctl status superdesk

###important tools
atom
pycharm
catfish
nomachine
robo3t or compass or nosqlbooster
gufw


### stop /start mongodb
sudo service mongod start
sudo service mongod stop
sudo service mongod restart
sudo service mongod status


###Check for  server python modification errors
##change dir to 
cd /opt/superdesk/server
gunicorn superdesk

###pycharm installation after download 
sudo su
cd Downloads/pycharm-community-xxxx.x.x/bin
./pycharm.sh

### detect all os open ports
nmap -p- localhost


### webpacking client after modification 
##change dir to 
cd /opt/superdesk/client
grunt build

### run client development server
grunt server 
-->> onpen in browser http://localhost:9000



###Create Users
source /opt/superdesk/activate.sh
cd /opt/superdesk/server
./manage.py users:create -u admin -p admin -e 'admin@example.com' --admin
./manage.py users:create -u amac -p amac -e 'amac@example.com' --admin


###compaction all collection in MongoDb
use testDbName;
db.getCollectionNames().forEach(function (collectionName) {
    print('Compacting: ' + collectionName);
    db.runCommand({ compact: collectionName });
});


### repair mongoDb 
db.repairDatabase()

## Restore MongoDB From Dumb Files
mongorestore <Directory of Dumb Folder>

### open the pycharm without configuration errors after installation form the "ubuntu software"
sudo pycharm-community


##Login to Ubuntu server from windows by a command line
######################################################
-Run a command line as Administrator
-Write : ssh newsadmin@10.2.210.23
-Choose (yes).
-Write Password.
-to reboot ubuntu system try this command:
sudo reboot now
-----------------------------------------

